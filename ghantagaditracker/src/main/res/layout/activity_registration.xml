<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.RegistrationActivity"
    android:fitsSystemWindows="false">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/dim_registration_actionbar"
            android:background="?attr/colorPrimary"
            android:gravity="center"
            android:padding="@dimen/padding_margin_layout_double">

            <ImageView
                android:contentDescription="@string/app_name"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:src="@mipmap/ic_launcher_round"
                android:background="@drawable/round_background_white"/>

        </LinearLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <ViewStub
        android:id="@+id/view_stub"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="180dp"/>

    <!--<include-->
        <!--android:id="@+id/view_registration"-->
        <!--layout="@layout/content_registration"-->
        <!--android:visibility="visible"/>-->

    <!--<include-->
        <!--android:id="@+id/view_otp"-->
        <!--layout="@layout/content_registration_otp"-->
        <!--android:visibility="gone"/>-->

    <!--<include-->
        <!--android:id="@+id/view_verify_contact"-->
        <!--layout="@layout/content_verify_contact"-->
        <!--android:visibility="gone"/>-->

</androidx.coordinatorlayout.widget.CoordinatorLayout>