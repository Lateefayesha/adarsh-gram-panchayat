<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/padding_margin_layout_double"
    android:elevation="@dimen/cardview_radius"
    android:layout_marginBottom="@dimen/padding_margin_layout_double">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_margin_layout">

        <ImageButton
            android:id="@+id/notification_clear"
            android:contentDescription="@string/app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_clear_black_24dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"/>

        <TextView
            android:id="@+id/notification_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@id/notification_clear"
            android:paddingStart="@dimen/padding_margin_layout"
            android:paddingEnd="@dimen/padding_margin_layout"/>

        <TextView
            android:id="@+id/notification_date_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/padding_margin_layout"
            android:textSize="@dimen/secondary_text_size"
            android:layout_below="@id/notification_text"
            android:textColor="@color/text_color_dividers"
            android:paddingStart="@dimen/padding_margin_layout"
            android:paddingEnd="@dimen/padding_margin_layout"/>

    </RelativeLayout>

</androidx.cardview.widget.CardView>
